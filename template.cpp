#include <bits/stdc++.h>
// must already know std lib + container first, not recommended for beginner.

using namespace std;

int main() {
	// Fast IO
	// ios::sync_with_stdio(0);
	// cin.tie(0);
	// ios_base::sync_with_stdio(false); // or can use this

    cout << "Hello World" << endl;

    return 0;
}
